# Setup
#   docker compose up -d
#   make db/reset

services:
  postgres:
    image: postgres:16.3-alpine
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=password
    volumes:
      - postgres-volume:/var/lib/postgresql/data

volumes:
  postgres-volume:
